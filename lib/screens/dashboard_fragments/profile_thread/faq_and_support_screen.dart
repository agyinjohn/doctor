import 'package:flutter/material.dart';

class FaqAndSupportScreen extends StatelessWidget {
  const FaqAndSupportScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text('FAQ & Support Page'),
      ),
    );
  }
}
